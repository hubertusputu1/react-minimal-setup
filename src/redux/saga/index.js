import { all, takeLatest } from 'redux-saga/effects';

// import { FETCH_POKEMON, FETCH_SINGLE_POKEMON } from '../type/type.form';

// import { sagaFetchPokemons, sagaFetchSinglePokemon } from './saga.form';

export default function* IndexSaga() {
  // yield all([takeLatest(FETCH_POKEMON, sagaFetchPokemons)]);
  // yield all([takeLatest(FETCH_SINGLE_POKEMON, sagaFetchSinglePokemon)]);
}
